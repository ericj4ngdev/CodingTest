#include<bits/stdc++.h>
using namespace std;

int h,w;
int a[504];
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    cin >> h >> w;
    for(int i = 0; i < w; i++)
    {
        cin >> a[i];
    }

    // 높이 체크. 가장 낮은 높이
    // 양 끝에 높이?
    // 사이 공간이 있다는 걸 어떻게 인지할까??
    // 스택?
    // 


    return 0;
}

