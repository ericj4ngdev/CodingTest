#include<bits/stdc++.h>
using namespace std;

int main()
{
    vector<vector<int>> v;
    v.emplace_back(100);
    // v.push_back(100);

    int a = 0;

    
    // 물리만으로는 한계가 있다.
    // 물리 -> 논리 메모리 로 관리하는 

    // 램 : 물리
    // CPU : 논리

    // 가상메모리 : 물리적 공간을 극복하기 위해 쓰는 메모리
    // CPU -> 논리 -> MMU -> 페이지 테이블 -> 물리적 공간에 맞춰주는??

    return 0;
}
